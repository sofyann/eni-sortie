{% extends 'base.html.twig' %}

{% block body %}
    <h1>Modification de la sortie</h1>

    {{ form_start(tripForm, {attr: {novalidate: 'novalidate'}}) }}
    {{ form_widget(tripForm) }}

    <div class="row">

        <div class="col-sm-10 offset-2">
            <button class="btn btn-success">Modifier</button>
        </div>
    </div>

    {{ form_end(tripForm) }}

{% endblock %}

{% block title %}Modifier une sortie{% endblock %}
